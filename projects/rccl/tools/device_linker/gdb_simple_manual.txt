# Simple GDB commands to run manually
# Copy and paste these into gdb after running: ./tools/device_linker/gdb_interactive.sh

# Step 1: Set breakpoint where error is detected
break DWARFUnit::tryExtractDIEsIfNeeded

# Step 2: Run the program
run -o /tmp/test_output.o --dispatcher build/release/device_linker_output/dispatcher_device.elf --host-table build/release/hipify/gensrc/host_table.cpp --target gfx942:xnack-:sramecc+ --input-dir build/release/specialized_objs/

# Step 3: When it breaks, check what's being read
# (gdb will stop at the breakpoint)
print this
print *this

# Step 4: Look at local variables to see what data is being read
info locals

# Step 5: Check the stack trace to see the call chain
bt

# Step 6: Go up the stack to see where the data comes from
up
info locals
up
info locals

# Step 7: Continue to see if it breaks again
continue
